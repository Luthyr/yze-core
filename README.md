# yze-core
Core game module for the YZE game system.

// Hook: "yzeCorePushedRoll"
// Fires after a successful push reroll updates the chat message.
// Context: { actor, message, rollState, roll, previous: { rollState, dice, successes } }

// YZE Core v0.3.0
// Stable: push mechanics

// YZE Core v0.2.0
// Stable: attribute + skill rolls, chat cards, rollState persisted

// YZE Core v0.1.0
// Stable: base data structures
