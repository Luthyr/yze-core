<div class="yze-roll-card yze-npc-roll-card">
  <h3>{{poolName}} Roll</h3>
  {{#if pushed}}
    <div>PUSHED</div>
  {{/if}}
  <div>{{actorName}}</div>
  <div>Dice: {{dice.length}}</div>
  <ul>
    {{#each dice}}
      <li>{{this}}</li>
    {{/each}}
  </ul>
  <div>Successes: {{successes}}</div>
</div>
