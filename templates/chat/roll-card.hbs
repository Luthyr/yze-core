<div class="yze-roll-card">
  <h3>{{title}}</h3>
  {{#if pushed}}
    <div>PUSHED</div>
  {{/if}}
  <div>{{actorName}}</div>
  <div>Dice Pool: {{diceCount}}</div>
  <ul>
    {{#each dice}}
      <li>{{this}}</li>
    {{/each}}
  </ul>
  <div>Successes: {{successes}}</div>
</div>
